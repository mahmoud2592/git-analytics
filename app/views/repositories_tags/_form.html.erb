<%= form_with(model: repositories_tag) do |form| %>
  <% if repositories_tag.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(repositories_tag.errors.count, "error") %> prohibited this repositories_tag from being saved:</h2>

      <ul>
        <% repositories_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :repository_id, style: "display: block" %>
    <%= form.number_field :repository_id %>
  </div>

  <div>
    <%= form.label :tag_id, style: "display: block" %>
    <%= form.number_field :tag_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
